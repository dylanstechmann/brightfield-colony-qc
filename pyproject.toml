[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "colonyqc"
version = "0.3.0"
description = "Colony image features, synthetic morphology demos and a NIST iPSC data importer."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Dylan Stechmann"}]
dependencies = [
  "numpy>=1.24,<3",
]

[project.scripts]
colonyqc = "colonyqc.cli:main"

[project.optional-dependencies]
images = ["Pillow>=10,<13"]

[tool.setuptools.packages.find]
where = ["src"]
